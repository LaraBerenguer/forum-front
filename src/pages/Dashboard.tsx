import { Link } from "react-router";

const Dashboard = () => {
    return (
        <>
        <div>Dashboard</div>
        <Link to={"/"}>Go back Home</Link>
        </>
    );
};

export default Dashboard;